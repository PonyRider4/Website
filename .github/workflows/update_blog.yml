name: Auto Update Blog

# 触发条件：当你推送到 main 分支时
on:
  push:
    paths:
      - 'posts/**'       # 只有 posts 文件夹变动时才运行
      - 'auto_gen.py'    # 或者修改了脚本时运行

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # 赋予写权限，以便提交 json 文件

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Run generator script
      run: python auto_gen.py

    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        git add posts.json
        # 如果 posts.json 有变化，才提交
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 自动更新文章列表" && git push)
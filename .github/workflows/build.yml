name: Build Blog Data

on:
  push:
    paths:
      - 'posts/**'      # 只有 posts 文件夹变动时运行
      - 'build.py'      # 或者修改了脚本时运行
      - 'index.html'    
  workflow_dispatch:    # 允许手动点击运行

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # 必须给写权限！

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Run build script
      run: python build.py

    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        # 检查 posts.json 是否有变化，有变化才提交
        git add posts.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 自动构建 posts.json" && git push)
